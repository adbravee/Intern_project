{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=ItemDB;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
  },

  // Hard-coded admin credentials — change before deploying to production!
  "Admin": {
    "Username": "admin",
    "Password": "Admin@123"
  },

  // Optional: set to true if you want the app to auto-apply EF migrations on startup.
  // If your SQL Server/database isn't ready yet, keep this false (recommended for smooth startup).
  "AutoMigrateOnStartup": false,

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
